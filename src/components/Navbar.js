import React from "react";
import styled from "styled-components";

const StyledWrapper = styled.div`
  background: red;
  width: 300%;
  height: 70px;
`;

const Navbar = () => {
  return <StyledWrapper>Navbar</StyledWrapper>;
};

export default Navbar;
